# Skill: VS Code Extension Development

## When To Use
- Changing commands, activation events, views, or extension entrypoints.

## Steps
1. Keep command IDs stable.
2. Align package.json contributions with registered commands.
3. Package before distribution.

## Checks
- Run `npm run compile`.
- Run `npm run package`.