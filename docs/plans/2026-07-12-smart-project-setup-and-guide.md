# Smart Project Setup And Guide Plan

1. Add a deterministic project analyzer and a small pack catalog.
2. Materialize selected bundled upstream files without network access.
3. Apply internal INI Brain features and mirror focused skills to detected agents.
4. Expose preview/apply through VS Code and MCP.
5. Add behavior-focused smoke tests for empty, frontend, large, and multi-agent workspaces.
6. Build an Arabic RTL HTML guide with reusable CSS and illustrated SVG screenshots.
7. Verify links, responsive layout, compile, smoke tests, public audit, and VSIX packaging.

